__pycache__/
*.pyc
.DS_Store
# Local only: photo copies from get-photos.sh (the live page loads the retailer photos)
images/*.jpg
images/photos.js
# Per-run baseline written by the skills' check.py --snapshot
.slots-before.json
